Write a version control system. Content can be checked in and checked out. Changes must be kept track of, so that rollbacks are possible.
http://en.wikipedia.org/wiki/Revision_control

Commands:

- user login username
- create repo pathToRepo
- add files
- commit -m "message"
- log : commit version|username|timestamp|message
- checkout
- delete commit